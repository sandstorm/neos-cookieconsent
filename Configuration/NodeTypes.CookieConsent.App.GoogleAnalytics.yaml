'Sandstorm.CookieConsent:App.GoogleAnalytics':
  superTypes:
    'Sandstorm.CookieConsent:App': true
  ui:
    label: Google Analytics
  klaro:
    # This name can be used as a reference in script-tags or fonts that should be lazy-loaded. See klaro documentation.
    # This value should be considered API as it will be referenced in implementations. If you change it, please release
    # a major version as it should be considered a breaking change.
    name: googleAnalytics
    cookies:
      - ['_ga', "/"]
      - ['_gat', "/"]
      - ['_gid', "/"]
    purposes:
      - tracking
  properties:
    title:
      defaultValue: GoogleAnalytics
    description:
      defaultValue: Tracking des Nutzerverhaltens (z.B. für gezieltere Werbekampagnen).

